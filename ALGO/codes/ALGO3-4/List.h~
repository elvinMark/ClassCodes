#include <stdio.h>
#include <stdlib.h>

typedef struct node{
  int data;
  struct node* next;
}node;

typedef node* link;

link NEW(int data,link n);
void insertNext(link t,link x);
link deleteNext(link t);
void printList(link h);
